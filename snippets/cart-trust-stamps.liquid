<ul class="cart-trust{% if compact %} cart-trust--compact{% endif %}" role="list">
  <li class="cart-trust__item">
    <span class="cart-trust__mark" aria-hidden="true">01</span>
    <span>{{ settings.cart_benefit_1 | escape }}</span>
  </li>
  <li class="cart-trust__item">
    <span class="cart-trust__mark" aria-hidden="true">02</span>
    <span>{{ settings.cart_benefit_2 | escape }}</span>
  </li>
  <li class="cart-trust__item">
    <span class="cart-trust__mark" aria-hidden="true">03</span>
    <span>{{ settings.cart_benefit_3 | escape }}</span>
  </li>
  <li class="cart-trust__item">
    <span class="cart-trust__mark" aria-hidden="true">04</span>
    <span>{{ settings.cart_benefit_4 | escape }}</span>
  </li>
</ul>
